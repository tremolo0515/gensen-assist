# ポケスリ厳選サポーター - 開発ガイド

---

## 1. プロジェクト概要

**ツール名**: ポケスリ厳選サポーター
**目的**: ポケモンスリープのプレイヤーが「今厳選すべきポケモン」を把握できるWebツール
**オーナー**: 一弓（いっきゅう）さん
**リポジトリ**: `/Users/ki/dev/gensen-assist/`

### ユーザーの操作フロー

1. 食材19種類のチェックボックスで「厳選済み」をチェック
2. 現在のなべ容量を入力
3. ツールが計算 → 今厳選すべきポケモンを優先度順で表示

### 状態の保持

- ユーザー入力はローカルストレージに保存（サーバー不要）

---

## 2. 技術スタック

| 項目 | 採用 | 備考 |
|------|------|------|
| フレームワーク | Next.js (App Router) | — |
| 言語 | TypeScript | — |
| スタイリング | Tailwind CSS | — |
| 状態管理 | React useState + localStorage | サーバー不要 |
| デプロイ | Vercel | — |
| パッケージ管理 | npm | — |

---

## 3. アーキテクチャ方針

- **フロントエンドのみ**。バックエンド・APIルートは作らない
- ゲームデータ（食材・レシピ・ポケモン対応表）は**静的JSONファイル**として `/data/` に配置
- ロジック（計算・フィルタ）は `/lib/` に分離してテスト可能にする
- コンポーネントは小さく保つ。1ファイル1責務

---

## 4. ディレクトリ構成

```
gensen-assist/
├── CLAUDE.md
├── docs/                    ← 仕様ドキュメント（このAIが参照する）
│   ├── overview.md          ← ツール概要
│   ├── features.md          ← 機能一覧・MVP定義
│   ├── game-mechanics.md    ← ゲーム仕様（食材・レシピ・枠システム）
│   └── data-model.md        ← データ構造定義
├── data/                    ← 静的ゲームデータ（JSON）
│   ├── ingredients.json     ← 食材19種類
│   ├── recipes-curry.json   ← カレー・シチューレシピ
│   ├── recipes-salad.json   ← サラダレシピ
│   ├── recipes-dessert.json ← デザート・ドリンクレシピ
│   └── pokemon.json         ← ポケモン×食材対応表（ingredient1/2/3）
├── src/
│   ├── app/                 ← Next.js App Router
│   ├── components/          ← UIコンポーネント
│   └── lib/                 ← 計算ロジック・型定義
└── public/
```

---

## 5. ゲーム仕様（重要）

### 食材の枠システム（A/B/C）

| 枠 | 食材候補 | 解放Lv | MVPでの扱い |
|----|---------|--------|------------|
| 1枠 | 食材A | Lv.1 | ✅ 担当とみなす |
| 2枠 | 食材B | Lv.30 | ✅ 担当とみなす |
| 3枠 | 食材C | Lv.60 | ❌ 担当としない |

- 3枠（食材C）はLv.60以上でないと解放されず、量も少ないため担当としない
- ポケモン提案時は**1枠または2枠に目的の食材を持つポケモンのみ**表示する

### MVPの前提

- チェック済みの食材 = **無限供給**とみなす
- 個体差（個体ごとの持参量）は考慮しない
- 全19食材がMVP対象

### なべ容量と作れるレシピの関係

- なべ容量 = 1回の料理で投入できる食材の合計個数
- レシピごとに必要食材の合計数が決まっており、なべ容量が足りないレシピは作れない

---

## 6. 推薦ロジック（コアアルゴリズム）

**優先度の軸: エナジー最大レシピ優先**（レシピ数ではなくエナジーで判断）

```
入力:
  - checkedIngredients: string[]   // チェック済み食材ID
  - potCapacity: number            // なべ容量

処理:
  1. 未チェック食材ごとに「その食材をチェックしたと仮定」して計算
     → 新たに作れるようになるレシピを特定
       = 全食材が揃う かつ totalCount <= potCapacity

  2. 新たに解放されるレシピのうち、最大エナジーを「その食材のスコア」とする

  3. スコア（最大エナジー）の高い食材順にランキング

  4. 各食材の「ingredient1（A枠）またはingredient2（B枠）に持つポケモン」を表示
     ※ ingredient3（C枠）のみのポケモンは除外

出力:
  - 優先度順のおすすめ食材 × 担当ポケモン一覧
  - 各提案: ポケモン名 / 担当食材（A枠 or B枠） / 解放できる最大エナジーレシピ名 / エナジー
```

---

## 7. 開発ルール

- `docs/` のファイルを必ず参照してから実装を始める
- データの追加・修正は `data/*.json` のみ変更する（コードに直書きしない）
- 計算ロジックは `src/lib/` に集約する
- コンポーネントにロジックを書かない
- TypeScriptの型を先に定義してから実装する

---

## 8. 参照ドキュメント

詳細仕様は `docs/` を参照:

| ファイル | 内容 |
|---------|------|
| `docs/gensen-assist-overview.md` | ツール概要・差別化ポイント |
| `docs/gensen-assist-features.md` | MVP機能一覧・データフロー |
| `docs/gensen-assist-ui-spec.md` | 画面仕様・v0.devプロンプト |
| `docs/gensen-assist-game-data.md` | wikiから収集したゲームデータ・URL一覧 |

---

## 9. モックアップ

- **フォルダ**: `b_vPF2cjTQ4QO-1774078050598/`（v0.devが生成したNext.js + Tailwindのコード）
- UIの参考として使用。実装時にこのコードをベースにするか選択する

---

## 10. wikiデータ参照ルール

ゲームデータが必要になったら:

1. まず `docs/gensen-assist-game-data.md` を確認（取得済みデータのキャッシュ）
2. なければ WebFetch でwikiから取得（ブロックなし）
   - 食材担当ポケモン: `https://wikiwiki.jp/poke_sleep/食材/{食材名}`
   - レシピ一覧: `https://wikiwiki.jp/poke_sleep/料理/レシピの一覧`
3. 取得後は `gensen-assist-game-data.md` に追記してキャッシュする
4. ユーザーがwikiのURLを渡してきた場合も同様に取得・追記する
